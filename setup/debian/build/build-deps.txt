# apt build deps for setup/debian packages (nwg-dock-hyprland).
# Used by `make build-deps` on the host and by build/Containerfile for the
# podman test image. Keep one package per line; comments and blanks ignored.
build-essential
ca-certificates
curl
file
git
gnupg
golang
libgtk-3-dev
libgtk-layer-shell-dev
libgtk-layer-shell0
pkg-config
ruby
ruby-dev
xz-utils
